@model ModelLayer.Models.ViewModel.PurchaseRequisitionListReportViewModelList

@{
    ViewBag.Title = "Purchase Requisition List Report";
    if (Session["Category"] != null)
    {
        if (Session["Category"].ToString() == "IsCategory")
        {
            Layout = "~/Views/Shared/_CategoryLayout.cshtml";
        }
    }
}

@*Script of DataTable  for Pagination*@
@*<script src="~/Scripts/jquery-1.12.4.js"></script>

<link href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" rel="stylesheet" />
<script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>*@

@*End Of Script of DataTable for Pagination*@

<style>
    .ClaimAmtList table {
        border-collapse: separate;
        width: 800px;
        max-width: 800px;
        overflow-x: scroll;
        display: block;
    }

    .ClaimAmtList thead, .ClaimAmtList tfoot {
        background-color: #EFEFEF;
    }

    .ClaimAmtList thead, .ClaimAmtList tbody, .ClaimAmtList tfoot {
        display: block;
    }

    .ClaimAmtList tbody {
        overflow-y: scroll;
        overflow-x: auto;
        max-height: 350px;
    }

    .ClaimAmtList td, .ClaimAmtList th {
        padding: 5px;
        min-width: 80px;
        height: 25px;
        border: inset 1px lightblue;
        /*overflow: hidden;*/
        text-overflow: ellipsis;
        max-width: 80px;
        /*word-break: break-all;*/
        text-align: center;
    }
</style>



<link href="~/Content/css/bootstrap-datetimepicker.min.css" rel="stylesheet" />
<link href="~/Content/css/bootstrap-datetimepicker.css" rel="stylesheet" />
<link href="~/Content/assets/css/font-awesome.min.css" rel="stylesheet" />
<link href="~/Content/assets/css/form-design.css" rel="stylesheet" />


<section class="right_side_main_part">
    <div class="row-fluid">
        @using (Html.BeginForm("GetPurchaseRequisitionList", "PurchaseRequisitionList", new { enctype = "multipart/form-data" }, FormMethod.Post, new { id = "Form_" }))
        {
            @Html.ValidationSummary(true)

            <div class="box span12 right_side_up_part">
                <div class="box-header creatProduct clearfix">
                    <h2>
                        <i class="icon-list-alt"></i>
                        <span class="break"></span>
                        Purchase Requisition List Report
                    </h2>
                </div>
                <div class="box-content nameDscrp">
                    <div class="form-horizontal">
                        <div class="NameDescription">
                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    Filter by Warehouse
                                </div>
                                @Html.DropDownListFor(model => model.WarehouseID, Model.WarehouseList)
                                @*<div class="col-sm-6">
                                        @if (ViewBag.PossibleSuppliers != null)
                                        {
                                            @Html.DropDownListFor(model => model.WarehouseID, ((IEnumerable<ModelLayer.Models.Supplier>)ViewBag.PossibleSuppliers).Select(option => new SelectListItem
                                        {
                                            Text = (option == null ? "None" : option.Name),
                                            Value = option.ID.ToString(),
                                            Selected = (Model != null) && (option.ID == Model.WarehouseID)
                                        }), "--All Suppliers--")
                                        }
                                    </div>*@
                            </div>



                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    From Date
                                </div>
                                <div class="col-sm-6">
                                    @*@Html.TextBox("FromDate", null, new { @readonly = "readonly", @class = "search", placeholder = "From" })*@
                                    @Html.TextBoxFor(model => model.FromDate, "{0:dd/MM/yyyy}", new { @readonly = "readonly", @required = "required" })
                                    @Html.HiddenFor(model => model.FromDate)
                                </div>
                            </div>
                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    To Date
                                </div>
                                <div class="col-sm-6">
                                    @*@Html.TextBox("ToDate", null, new { @readonly = "readonly", @class = "search", placeholder = "To" })*@
                                    @Html.TextBoxFor(model => model.ToDate, "{0:MM/dd/yyyy}", new { @readonly = "readonly", @required = "required" })
                                    @Html.HiddenFor(model => model.ToDate)
                                </div>
                            </div>

                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    Export In
                                </div>
                                <div class="col-sm-6">
                                    @Html.DropDownList("ExportOption", new List<SelectListItem>
                             {
                                     new SelectListItem{ Text="Excel", Value = "1" },
                                     new SelectListItem{ Text="Csv", Value = "2" },
                                     new SelectListItem{ Text="Pdf", Value = "3" }
                            })
                                </div>
                            </div>
                            <div class="clr"></div>
                            <button value="Search" type="submit" class="btnRemove btn btn-warning">Search</button>
                            @*<button id="btnPrint" class="btnPrint btn btn-success">Print</button>*@
                            @*<a id="Print" class="print btn btn-default" href="@Url.Action("GetPurchaseRequisitionList", "PurchaseRequisitionList")" target="_blank">Print</a>*@
                        </div>
                    </div>
                </div>
            </div>

        }
        <iframe id="txtArea1" style="display:none"></iframe>

        <button id="btnExport" onclick="fnExcelReport();" class="btnPrint btn btn-success btn-small">EXPORT TO EXCEL</button>
        <div class="box right_side_down_part">
            <div class="box-content">
                <div class="NameDescription">
                    <div id="divToPrint">
                        <table id="headerTable" class="table table-bordered table-striped table-condensed" border="1" cellpadding="2" cellspacing="0">
                            <thead>
                                <tr><td class="TD_Heading" style="text-align:center" colspan="12"><h2>Purchase Requisition List Report </h2></tdclass="TD_Heading"></tr>
                                <tr>
                                    <th>Sr.No.</th>
                                    <th>SKU ID</th>
                                    <th>SKU Name</th>
                                    <th>SKU Unit</th>
                                    <th>HSN Code</th>
                                    <th>Batch Code</th>
                                    @*<th style="min-width: 120px; max-width: 120px;">
                                            Supplier
                                        </th>*@
                                    <th>Brand Name</th>
                                    <th>Supplier</th>
                                    @*<th style="min-width: 100px; max-width: 100px; word-break: break-all"> Brand Name</th>*@
                                    <th>Quantity</th>
                                    <th>Quotation Date </th>
                                    @*<th style="text-align:right">UsualDeliveryTime</th>*@
                                    <th>Amount</th>
                                    <th>Last Purchase Quantity</th>
                                    <th>MRP</th>
                                    <th>Sale Price</th>
                                    <th>Reorder Hit Date</th>




                                    @*<th style="text-align:right">
                                           CreateDate
                                        </th>*@
                                    @*<th style="text-align:right"> Create Date</th>*@
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    var i = 1;
                                }
                                @*@foreach (var item in Model)*@

                                @foreach (var item in Model.lPurchaseRequisitionListReportViewModel)
                                {
                                    <tr>
                                        <td style="text-align: center; min-width: 50px; max-width: 50px; word-break: break-all">
                                            @i
                                            @{
                                                i = i + 1;
                                            }
                                        </td>
                                        @*<td style="min-width: 100px; max-width: 100px; word-break: break-all;">*@
                                        @*@(item.PurchaseOrderCode == null ? "" : item.PurchaseOrderCode)*@
                                        @*<a id="Print" style="color: #3C73DC; font-weight: bold; " href="@Url.Action("PrintOrder", "PurchaseOrders", new { id = @item.PurchaseOrderID })" target="_blank">@(item == null ? "" : item.PurchaseOrderCode)</a>
                                            </td>*@


                                        @*<td style="min-width: 120px; max-width: 120px;text-align:left">
                                                @(item.SupplierName == null ? "None" : item.SupplierName)
                                            </td>*@
                                        <td>@item.SKUID</td>
                                        <td>@item.SKUName</td>
                                        <td>@item.SKUUnit</td>
                                        <td>@item.HSNCode</td>
                                        <td>@item.BatchCode</td>
                                        <td>@item.Manufacturer</td>
                                        <td>@item.Supplier</td>
                                        @*<td style="min-width: 100px; max-width: 100px; word-break: break-all">
                                                @item.BrandName
                                            </td>*@
                                        <td>@item.Quantity</td>
                                        @*<td style="color: #DB4437; font-weight: bold; word-break: break-all">
                                                <a id="Print" style="color: #DB4437; font-weight: bold" href="@Url.Action("PrintGR", "Invoices", new { InvoiceID = @item.InvoiceID })" target="_blank">@item.InvoiceCode</a>
                                            </td>*@
                                        <td>@item.QuotationDate</td>
                                        @*<td style="text-align:right">@item.UsualDeliveryTime</td>*@
                                        <td style="text-align:center">@item.Amount</td>
                                        <td style="text-align:center">@item.LastPurchaseQuantity</td>
                                        <td style="text-align:center">@item.MRP</td>
                                        <td style="text-align:center">@item.SalePrice</td>
                                        <td style="text-align:center">@item.ReorderHitDate</td>






                                    </tr>



                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</section>
<script type="text/javascript">

    $(document).ready(function () {
        $('#ToDate').datepicker({ dateFormat: 'mm/dd/yy', maxDate: 'now' });
        $('#FromDate').datepicker({ dateFormat: 'mm/dd/yy' });

        $('#Search').click();
        //$("#Export").on('click', function () {
        //    var WarehouseID = $("select#WarehouseID option:selected").val();

        //    var print = 0;
        //    var option = 0;
        //    //var WarehouseID = 0

        //    if ($("select#WarehouseID option:selected").val() > 0) {
        //        WarehouseID = $("select#WarehouseID option:selected").val();
        //    }

        //    if ($("select#ExportOption option:selected").val() > 0) {
        //        option = $("select#ExportOption option:selected").val();
        //    }
        //    var toDate = $('#ToDate').val();
        //    var fromDate = $('#FromDate').val();

        //    window.location.href = "/OutOfStockReport/Export?&FromDate=" + fromDate + "&ToDate=" + toDate + "&option=" + option + "&WarehouseID=" + WarehouseID;

        //});
    });
</script>



@*<script type="text/javascript">
            function fnExcelReport() {
                var tab_text = "<table border='2px'><tr bgcolor='#87AFC6'>";
                var textRange; var j = 0;
                tab = document.getElementById('headerTable'); // id of table


                for (j = 0; j < tab.rows.length; j++) {
                    tab_text = tab_text + tab.rows[j].innerHTML + "</tr>";
                    //tab_text=tab_text+"</tr>";
                }

                tab_text = tab_text + "</table>";
                tab_text = tab_text.replace(/<A[^>]*>|<\/A>/g, "");//remove if u want links in your table
                tab_text = tab_text.replace(/<img[^>]*>/gi, ""); // remove if u want images in your table
                tab_text = tab_text.replace(/<input[^>]*>|<\/input>/gi, ""); // reomves input params

                var ua = window.navigator.userAgent;
                var msie = ua.indexOf("MSIE ");

                if (msie > 0 || !!navigator.userAgent.match(/Trident.*rv\:11\./))      // If Internet Explorer
                {
                    txtArea1.document.open("txt/html", "replace");
                    txtArea1.document.write(tab_text);
                    //txtArea1.document.AddHeader("PURCHASE ORDER HISTORY")
                    txtArea1.document.close();
                    txtArea1.focus();
                    sa = txtArea1.document.execCommand("SaveAs", true, "Say Thanks to Sumit.xls");
                }
                else {//other browser not tested on IE 11
                    // sa = window.open('data:application/vnd.ms-excel,' + encodeURIComponent(tab_text));
                    // sa.document.title = "your new title";

                    //new added by Priti
                    let a = $("<a />", {
                        href: 'data:application/vnd.ms-excel,' + encodeURIComponent(tab_text),
                        download: "PurchaseRequisitionList.xls"
                    })
                        .appendTo("body")
                        .get(0)
                        .click();
                    e.preventDefault();
                }

                return (sa);
            }

    </script>*@




<script type="text/javascript">
    function fnExcelReport() {
        var tab_text = '<table border="1px" style="font-size:20px" ">';
        var textRange;
        var j = 0;
        var tab = document.getElementById('headerTable'); // id of table
        var lines = tab.rows.length;

        // the first headline of the table
        if (lines > 0) {
            tab_text = tab_text + '<tr bgcolor="#DFDFDF">' + tab.rows[0].innerHTML + '</tr>';
        }

        // table data lines, loop starting from 1
        for (j = 1; j < lines; j++) {
            tab_text = tab_text + "<tr>" + tab.rows[j].innerHTML + "</tr>";
        }




        var fileName = 'Test file.xls';
        var blob = new Blob([tab_text], {
            type: "application/xls;charset=utf-8;"
        });

        if (window.navigator.msSaveBlob) {
            // FOR IE BROWSER
            navigator.msSaveBlob(blob, fileName);
        } else {
            // FOR OTHER BROWSERS
            var csvUrl = URL.createObjectURL(blob);



            //new added by Priti
            let a = $("<a />", {
                //href: 'data:application/vnd.ms-excel,' + encodeURIComponent(blob),
                href: csvUrl,
                download: "PurchaseRequisitionList.xls"
            })
                .appendTo("body")
                .get(0)
                .click();
            //$('#exportbutton').attr({
            //    'download': fileName,
            //    'href': csvUrl
            //});
        }
    }
</script>


@*Add for Pagination*@
@*<script type="text/javascript">
    $(document).ready(function () {
        $('#headerTable').DataTable();


    });
</script>*@
@*End Of Pagination*@


@*<script type="text/javascript">
        $(function () {
            $("#Search").click(function () {
                var WarehouseID = $("select#WarehouseID option:selected").val();
                //window.location = "/WastageReport/WastageReportNotePrint?WarehouseID=" + WarehouseID;
                window.location = "/OutOfStockReport/GetOutOfStockReport?WarehouseID=" + WarehouseID;
            });
        });

    </script>*@
