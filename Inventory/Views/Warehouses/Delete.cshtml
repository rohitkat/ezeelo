@model ModelLayer.Models.Warehouse

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Warehouse</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">Warehouse Code</div>
    <div class="display-field">@Model.WarehouseCode</div>

    <div class="display-label">GST Number</div>
    <div class="display-field">@Model.GSTNumber</div>

    <div class="display-label">Service Number</div>
    <div class="display-field">@Model.ServiceNumber</div>

    <div class="display-label">Near by Transport</div>
    <div class="display-field">@Model.NearbyTransport</div>

    <div class="display-label">Measurement</div>
    <div class="display-field">@Model.Measurement</div>

    <div class="display-label">Floor Space</div>
    <div class="display-field">@Model.FloorSpace</div>

    <div class="display-label">Volume</div>
    <div class="display-field">@Model.Volume</div>

    <div class="display-label">Custom Entry</div>
    <div class="display-field">@Model.CustomEntry</div>

    <div class="display-label">Custom Exit</div>
    <div class="display-field">@Model.CustomExit</div>

    <div class="display-label">Is Active</div>
    <div class="display-field">@Model.IsActive</div>

    <div class="display-label">Create Date</div>
    <div class="display-field">@String.Format("{0:g}", Model.CreateDate)</div>

    <div class="display-label">Create By</div>
    <div class="display-field">@Model.CreateBy</div>

    <div class="display-label">Modify Date</div>
    <div class="display-field">@String.Format("{0:g}", Model.ModifyDate)</div>

    <div class="display-label">Modify By</div>
    <div class="display-field">@Model.ModifyBy</div>

    <div class="display-label">Network IP</div>
    <div class="display-field">@Model.NetworkIP</div>

    <div class="display-label">Device Type</div>
    <div class="display-field">@Model.DeviceType</div>

    <div class="display-label">Device ID</div>
    <div class="display-field">@Model.DeviceID</div>

    <div class="display-label">Area</div>
    <div class="display-field">@(Model.Area == null ? "None" : Model.Area.Name)</div>

    <div class="display-label">Bulk Logs</div>
    <div class="display-field">@(Model.BulkLogs == null ? "None" : Model.BulkLogs.Count.ToString())</div>

    <div class="display-label">Business Detail</div>
    <div class="display-field">@(Model.BusinessDetail == null ? "None" : Model.BusinessDetail.Name)</div>

    <div class="display-label">Franchise</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.Franchise).ToString()</div>

    <div class="display-label">Personal Detail</div>
    <div class="display-field">@(Model.PersonalDetail == null ? "None" : Model.PersonalDetail.LastName)</div>

    <div class="display-label">Personal Detail 1</div>
    <div class="display-field">@(Model.PersonalDetail1 == null ? "None" : Model.PersonalDetail1.LastName)</div>

    <div class="display-label">Personal Detail 2</div>
    <div class="display-field">@(Model.PersonalDetail2 == null ? "None" : Model.PersonalDetail2.LastName)</div>

    <div class="display-label">States</div>
    <div class="display-field">@(Model.States == null ? "None" : Model.States.Count.ToString())</div>

    <div class="display-label">Citys</div>
    <div class="display-field">@(Model.Citys == null ? "None" : Model.Citys.Count.ToString())</div>

    <div class="display-label">Pincodes</div>
    <div class="display-field">@(Model.Pincodes == null ? "None" : Model.Pincodes.Count.ToString())</div>

    <div class="display-label">Shop Products</div>
    <div class="display-field">@(Model.ShopProducts == null ? "None" : Model.ShopProducts.Count.ToString())</div>

    <div class="display-label">Shop Product Charges</div>
    <div class="display-field">@(Model.ShopProductCharges == null ? "None" : Model.ShopProductCharges.Count.ToString())</div>

    <div class="display-label">Temp Shop Products</div>
    <div class="display-field">@(Model.TempShopProducts == null ? "None" : Model.TempShopProducts.Count.ToString())</div>

    <div class="display-label">Proprietory Products</div>
    <div class="display-field">@(Model.ProprietoryProducts == null ? "None" : Model.ProprietoryProducts.Count.ToString())</div>

    <div class="display-label">Shop Menu Priorities</div>
    <div class="display-field">@(Model.ShopMenuPriorities == null ? "None" : Model.ShopMenuPriorities.Count.ToString())</div>

    <div class="display-label">Premium Shops Priorities</div>
    <div class="display-field">@(Model.PremiumShopsPriorities == null ? "None" : Model.PremiumShopsPriorities.Count.ToString())</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


