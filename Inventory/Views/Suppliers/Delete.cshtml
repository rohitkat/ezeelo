@model ModelLayer.Models.Supplier

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Supplier</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">SupplierCode</div>
    <div class="display-field">@Model.SupplierCode</div>

    <div class="display-label">ContactPerson</div>
    <div class="display-field">@Model.ContactPerson</div>

    <div class="display-label">Address</div>
    <div class="display-field">@Model.Address</div>

    <div class="display-label">Mobile</div>
    <div class="display-field">@Model.Mobile</div>

    <div class="display-label">Landline</div>
    <div class="display-field">@Model.Landline</div>

    <div class="display-label">Email</div>
    <div class="display-field">@Model.Email</div>

    <div class="display-label">FAX</div>
    <div class="display-field">@Model.FAX</div>

    <div class="display-label">GSTNumber</div>
    <div class="display-field">@Model.GSTNumber</div>

    <div class="display-label">PAN</div>
    <div class="display-field">@Model.PAN</div>

    <div class="display-label">Website</div>
    <div class="display-field">@Model.Website</div>

    <div class="display-label">CC</div>
    <div class="display-field">@Model.CC</div>

    <div class="display-label">IsActive</div>
    <div class="display-field">@Model.IsActive</div>

    <div class="display-label">CreateDate</div>
    <div class="display-field">@String.Format("{0:g}", Model.CreateDate)</div>

    <div class="display-label">CreateBy</div>
    <div class="display-field">@Model.CreateBy</div>

    <div class="display-label">ModifyDate</div>
    <div class="display-field">@String.Format("{0:g}", Model.ModifyDate)</div>

    <div class="display-label">ModifyBy</div>
    <div class="display-field">@Model.ModifyBy</div>

    <div class="display-label">NetworkIP</div>
    <div class="display-field">@Model.NetworkIP</div>

    <div class="display-label">DeviceType</div>
    <div class="display-field">@Model.DeviceType</div>

    <div class="display-label">DeviceID</div>
    <div class="display-field">@Model.DeviceID</div>

    <div class="display-label">PurchaseOrders</div>
    <div class="display-field">@(Model.PurchaseOrders == null ? "None" : Model.PurchaseOrders.Count.ToString())</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


