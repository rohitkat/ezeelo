@model ModelLayer.Models.ViewModel.InvoicesReceivedReportViewModelList

@{
    ViewBag.Title = "InvoicesGenerateReport";
}
<script src="~/Content/js/jquery-1.9.1.min.js"></script>




<style>
    .ClaimAmtList table {
        border-collapse: separate;
        width: 800px;
        max-width: 800px;
        overflow-x: scroll;
        display: block;
    }

    .ClaimAmtList thead, .ClaimAmtList tfoot {
        background-color: #EFEFEF;
    }

    .ClaimAmtList thead, .ClaimAmtList tbody, .ClaimAmtList tfoot {
        display: block;
    }

    .ClaimAmtList tbody {
        overflow-y: scroll;
        overflow-x: auto;
        max-height: 350px;
    }

    .ClaimAmtList td, .ClaimAmtList th {
        padding: 5px;
        min-width: 80px;
        height: 25px;
        border: inset 1px lightblue;
        /*overflow: hidden;*/
        text-overflow: ellipsis;
        max-width: 80px;
        /*word-break: break-all;*/
        text-align: center;
    }
</style>

<script src="~/Content/js/jquery-ui-1.10.0.custom.min.js"></script>
<script src="~/Content/js/priti.js"></script>
<link href="~/Content/css/datepicker.css" rel="stylesheet" />
<link href="~/Content/css/jquery-ui-1.8.21.custom.css" rel="stylesheet" />


<section class="inventory-Report_main">
    <div class="row-fluid">
        @using (Html.BeginForm("GetInvoicesReceivedReport", "InvoicesReceivedReport", new { enctype = "multipart/form-data" }, FormMethod.Post, new { id = "Form_" }))
        {
            @Html.ValidationSummary(true)

            <div class="box span12 ">
                <div class="box-header creatProduct clearfix">
                    <h2>
                        <i class="icon-list-alt"></i>
                        <span class="break"></span>
                        Invoice Generate  Report
                    </h2>
                </div>
                <div class="box-content nameDscrp">
                    <div class="form-horizontal">
                        <div class="NameDescription">
                            @*<div class="control-group mrg-left">

        @Html.DropDownListFor(model => model.WarehouseID, Model.WarehouseList)*@


                            @*<div class="col-sm-6">
                 @if (ViewBag.PossibleSuppliers != null)
                 {
                     @Html.DropDownListFor(model => model.SupplierID, ((IEnumerable<ModelLayer.Models.Supplier>)ViewBag.PossibleSuppliers).Select(option => new SelectListItem
        {
            Text = (option == null ? "None" : option.Name),
            Value = option.ID.ToString(),
            Selected = (Model != null) && (option.ID == Model.SupplierID)
        }), "--All Suppliers--")
                 }
             </div>


                        </div>*@

                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    From Date
                                </div>
                                <div class="col-sm-6">
                                    @*@Html.TextBox("FromDate", null, new { @readonly = "readonly", @class = "search", placeholder = "From" })*@
                                    @Html.TextBoxFor(model => model.FromDate, "{0:dd/MM/yyyy}")
                                    @Html.HiddenFor(model => model.FromDate)
                                </div>
                            </div>
                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    To Date
                                </div>
                                <div class="col-sm-6">
                                    @*@Html.TextBox("ToDate", null, new { @readonly = "readonly", @class = "search", placeholder = "To" })*@
                                    @Html.TextBoxFor(model => model.ToDate, "{0:dd/MM/yyyy}")
                                    @Html.HiddenFor(model => model.ToDate)
                                </div>
                            </div>

                            <div class="control-group mrg-left">
                                <div class="col-sm-3">
                                    Export In
                                </div>
                                <div class="col-sm-6">
                                    @Html.DropDownList("ExportOption", new List<SelectListItem>
                             {
                                     new SelectListItem{ Text="Excel", Value = "1" },
                                     new SelectListItem{ Text="Csv", Value = "2" },
                                     new SelectListItem{ Text="Pdf", Value = "3" }
                            })
                                </div>
                            </div>
                            <div class="clr"></div>
                            <button value="Search" type="submit" class="btnRemove btn btn-warning">Search</button>
                            @*<button id="btnPrint" class="btnPrint btn btn-success">Print</button>*@
                            @*<a id="Print" class="print btn btn-default" href="@Url.Action("GetInvoicesReceivedReport", "InvoicesReceivedReport")" target="_blank">Print</a>*@
                        </div>
                    </div>
                </div>
            </div>

        }
        <iframe id="txtArea1" style="display:none"></iframe>

        <div class="export_btn"><button id="btnExport" onclick="fnExcelReport();" class="btnPrint btn btn-success btn-small">EXPORT TO EXCEL</button></div>
                <div class="box right_side_down_part">
                <div class="box-content">
                    <div class="NameDescription">
                        <div id="divToPrint">
                            <table id="headerTable" class=" table table-bordered table-striped table-condensed" border="1" cellpadding="2" cellspacing="0">
                                <thead>
                                    <tr>
                                        <td class="TD_Heading" style="text-align:center" colspan="12"><h2>  Invoice Generate  Report</h2></td>
                                    </tr>
                                    <tr>
                                        <th>Sr.No.</th>
                                        <th>SKU ID</th>
                                        <th>SKU Name</th>
                                        <th>SKU Unit</th>
                                        <th>HSN Code</th>
                                        @*<th style="min-width: 95px; max-width: 95px; text-align: left">
                                        SKUUnit
                                    </th>*@
                                        <th>Invoice No</th>
                                        <th>Invoice Date</th>
                                        @*<th style="min-width: 150px; max-width: 150px; text-align: left">
                                        ProductVarient
                                    </th>*@

                                        @*<th style="min-width:95px;max-width:95px;text-align:left">Invoice Paid Date</th>*@
                                        @*<th style="min-width: 95px; max-width: 95px; text-align: left">AvailableQuantity</th>*@
                                        @*<th style="text-align:right"> Supplier</th>*@
                                        <th>MRP</th>
                                        <th>Unit Price</th>
                                        <th>Invoice Value</th>
                                        <th>GST%</th>
                                        <th>CGST Amt</th>
                                        <th>SGST Amt</th>
                                        <th>Quantity</th>
                                        <th>Buyer Name</th>
                                        @*<th style="text-align:right">Total Tax</th>*@
                                        <th>Status</th>
                                        <th>PO NO.</th>
                                        <th>Entity</th>
                                        <th>City</th>
                                        <th>Franchise</th>
                                        @*<th style="min-width:95px ;max-width: 95px;text-align :left">Create Date</th>*@
                                    </tr>
                                </thead>
                                <tbody>
                                    @{
                                        var i = 1;
                                    }
                                    @*@foreach (var item in Model)*@

                                    @foreach (var item in Model.lInvoicesReceivedReportViewModel)
                                    {
                                        <tr>
                                            <td>
                                                @i
                                                @{
                                                    i = i + 1;
                                                }
                                            </td>
                                            @*<td style="min-width: 100px; max-width: 100px; word-break: break-all;">*@


                                            <td>@item.SKUID</td>
                                            <td>@item.SKUName</td>
                                            <td>@item.SKUUnit</td>
                                            <td>@item.HSNCode</td>
                                            @*<td style="min-width: 150px; max-width: 150px; text-align: left">@item.SKUUnit</td>*@
                                            @*<td style="min-width: 100px; max-width: 100px; word-break: break-all">@item.Manifecturer</td>*@
                                            @*<td style="text-align:right">@item.ProductVarient</td>*@
                                            <td>@item.InvoiceCode</td>
                                            <td>@item.InvoiceDate</td>
                                            @*<td style="min-width:75px; max-width: 75px; text-align: left">@item.InvoicePaidDate</td>*@
                                            <td>@item.MRP</td>
                                            <td>@item.UnitPrice</td>
                                            <td>@item.Amount</td>
                                            <td>@item.GSTInPer</td>
                                            <td>@item.CGSTAmount</td>
                                            <td>@item.SGSTAmount</td>
                                            <td>@item.Quantity</td>
                                            @*<td style="min-width:75px; max-width: 75px; text-align: left">@item.TotalTax</td>*@
                                            <td>@item.Supplier</td>
                                            <td>@item.Status</td>
                                            <td>@item.PONumber</td>
                                            <td>@item.Entity</td>
                                            <td>@item.City</td>
                                            <td>@item.Franchises</td>
                                            @*<td style="min-width:95px ;max-width: 95px;text-align :left">@item.CreateDate</td>*@
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</section>


<script>
    $(document).ready(function () {
        $("#ToDate").datepicker({
            dateFormat: 'dd/mm/yy',//check change
            changeMonth: true,
            changeYear: true
        });
        $("#FromDate").datepicker({
            dateFormat: 'dd/mm/yy',//check change
            changeMonth: true,
            changeYear: true
        });

        //$("#FromDate").val(twoDigitDate + "/" + twoDigitMonth + "/" + (fullDate.getFullYear() - 1) + ", " + hour + ":" + minute + " " + ampm);
        //$("#ToDate").val(twoDigitDate + "/" + twoDigitMonth + "/" + fullDate.getFullYear() + ", " + hour + ":" + minute + " " + ampm);

        //var toDate = $('#txtToDate').val();
        //var fromDate = $('#txtFromDate').val();

        $("#Export").on('click', function () {

            var print = 0;
            var option = 0;
            var supplierID = 0

            //if ($("select#SupplierID option:selected").val() > 0) {
            //    supplierID = $("select#SupplierID option:selected").val();
            //}

            //if ($("select#ExportOption option:selected").val() > 0) {
            //    option = $("select#ExportOption option:selected").val();
            //}
            var toDate = $('#ToDate').val();
            var fromDate = $('#FromDate').val();

            window.location.href = "/InvoicesReceivedReport/Export?&FromDate=" + fromDate + "&ToDate=" + toDate + "&option=" + option;

        });
    });

</script>
<script type="text/javascript">


    $("#Export").on('click', function () {

        var print = 0;
        var option = 1;
        var supplierID = 0

        if ($("select#SupplierID option:selected").val() > 0) {
            supplierID = $("select#SupplierID option:selected").val();
        }

        if ($("select#ExportOption option:selected").val() > 0) {
            option = $("select#ExportOption option:selected").val();
        }
        var toDate = $('#ToDate').val();
        var fromDate = $('#FromDate').val();

        window.location.href = "/InvoicesReceivedReport/Export?&FromDate=" + fromDate + "&ToDate=" + toDate + "&option=" + 1 + "&SupplierID=" + FrenchiseID;

    });

</script>


<script type="text/javascript">
    function fnExcelReport() {
        var tab_text = '<table border="1px" style="font-size:20px" ">';
        var textRange;
        var j = 0;
        var tab = document.getElementById('headerTable'); // id of table
        var lines = tab.rows.length;

        // the first headline of the table
        if (lines > 0) {
            tab_text = tab_text + '<tr bgcolor="#DFDFDF">' + tab.rows[0].innerHTML + '</tr>';
        }

        // table data lines, loop starting from 1
        for (j = 1; j < lines; j++) {
            tab_text = tab_text + "<tr>" + tab.rows[j].innerHTML + "</tr>";
        }




        var fileName = 'Test file.xls';
        var blob = new Blob([tab_text], {
            type: "application/xls;charset=utf-8;"
        });

        if (window.navigator.msSaveBlob) {
            // FOR IE BROWSER
            navigator.msSaveBlob(blob, fileName);
        } else {
            // FOR OTHER BROWSERS
            var csvUrl = URL.createObjectURL(blob);



            //new added by Priti
            let a = $("<a />", {
                //href: 'data:application/vnd.ms-excel,' + encodeURIComponent(blob),
                href: csvUrl,
                download: "InvoicesReceivedReport.xls"
            })
                .appendTo("body")
                .get(0)
                .click();
            //$('#exportbutton').attr({
            //    'download': fileName,
            //    'href': csvUrl
            //});
        }
    }
</script>

@*<script type="text/javascript">
    $(function () {
        $("#Search").click(function () {
            var FrenchiseID = $("select#WarehouseID option:selected").val();
            //window.location = "/WastageReport/WastageReportNotePrint?WarehouseID=" + WarehouseID;
            window.location = "/ FVPatnerAccounts/Indext?FrenchiseID =" + FrenchiseID;
        });
    });

</script>*@

